# 給与（kyuyo）は 今どんな状態か

2026-08-19 ／ 頭SHA = このファイルと同じコミット ／ 倉庫 = DB-test
**ここの数字は `node scripts/state-kyuyo.mjs` が正**（人が書き換えない・CIで毎回 数え直す）。

## まず3行

- **出来ている 6段 ／ 半分 3段 ／ 無い 0段 ／ 未測定 0段**（客が使う1周＝9段）
- **一番 危ない所** … **⑦振込データ**。ここだけ **読んだだけ**で、一度も動かしていない（未測定）。
  （※`framePageCount` の `return 0` は残す。**下絵0枚なら印刷ボタンが押せない**事を押して確かめた）
- **次に直すなら** … ⑦を **実データで1回 出して**、全銀のファイルの中身を1行ずつ読む。

---

## 客が使う1周（1段ずつ）

状態の言葉は **出来ている／半分／無い／未測定** の4つだけ。
確かめ方は **押した／読んだだけ／動かして数えた** の3つだけ。

### ① 会社を作る — 出来ている
- 確かめ方：**動かして数えた**（本物の app.js を動かして既定を作った）
- 数：聞く **7問**／会社が持つ物 **32個**
- 欠けている物：無し
- 危ない所：無し

### ② 人を入れる — 出来ている
- 確かめ方：**動かして数えた**
- 数：1人が持つ物 **62個**
- 欠けている物：無し
- 危ない所：無し

### ③ 勤怠を入れる — 出来ている
- 確かめ方：**動かして数えた**（日ごとに2日 入れて 読み直した／勤怠CSVは本物のエンジンに通して金額で見た）
- 数：日ごと **2日**／勤怠CSVが入れられる割増の欄 **7個**（エンジンが読む7個と一致）
- 欠けている物：**本物の勤怠ソフトが書き出したCSVでは 未検査**（見本CSVでしか流していない・2026-08-23）
- 危ない所：**受け口に欄が足りないと その時間は低い方の欄に落ちて 払い足りない**
  （2026-08-23 に実際に起きた＝時間外60時間超が 普通の残業として入っていた。
  今は `kyuyo/tests/csv-covers-engine.test.mjs` が エンジンと受け口の欄を突き合わせて 赤で止める）

### ④ 計算する — 出来ている
- 確かめ方：**動かして数えた**（実データ1人）
- 数：**総支給 260,000 − 控除 43,730 ＝ 手取り 216,270（差 0円）**
- 欠けている物：無し
- 危ない所：無し
- 覚え書き：最初 **0−0＝0 を「合っている」と数えていた**。欄の名前が違って（`monthly` ではなく **`base`**）
  計算が動いていなかった。**0どうしの一致は 検算ではない**ので、金額が動いた事も条件にした。

### ⑤ 明細を出す（紙） — 出来ている
- 確かめ方：**動かして数えた**（本番と同じ経路 `Render.build`）
- 数：紙 **1枚**／紙に出た数字 **260個**／
  **紙の「支給合計」260,000・「差引支給額」216,270 が 画面と一致**
- 欠けている物：無し
- 危ない所：無し
- 覚え書き：紙の字は **1文字ずつ離して組んである**（「差 引 支 給 額」）。
  そのまま探すと見つからず「未測定」になる。**空白を落としてから読む**。

### ⑥ 配る（Web明細） — 半分
- 確かめ方：**動かして数えた**（口が在る事まで）
- 数：配る口 **1個**／公開の関所 **在る**
- 欠けている物：**本物の倉庫が要る所は ここでは動かせない（未測定）**
- 危ない所：無し

### ⑦ 振込データ — 半分
- 確かめ方：**読んだだけ**
- 数：全銀の lib **1本**
- 欠けている物：**ここでは動かしていない（未測定）**
- 危ない所：無し

### ⑧ 年末調整 — 出来ている
- 確かめ方：**動かして数えた**（空で呼んで返る所まで）
- 数：計算の口 **1個**
- 欠けている物：実データでの1周は未測定
- 危ない所：**2026・2027年 専用**。ほかの年は「**その年の表は持っていません**」と出す（下の④参照）

### ⑨ 台帳・法定調書 — 半分
- 確かめ方：**動かして数えた**（口が在る事まで）
- 数：作る口 **5個**（賃金台帳・労働者名簿・資格・算定・月額変更）
- 欠けている物：**実データで出した紙は未測定**
- 危ない所：無し

---

## 深く見た6つ

**数を書く時の決まり**：**どこを数えたか**を必ず一緒に書く。**0件と未測定を混ぜない**。
`silent-catch.mjs` が数えるのは **23本**（給与7本・請求書14本・共有2本／**実物のパス**は道具が毎回 出す）。
それ以外は**未測定**。**書いた名前が無ければ赤**（黙って飛ばすと「見ていないのに0件」になる）。

| # | 見る所 | 数 | どう見たか |
|---|--------|----|-----------|
| ① | 呼ばれていない物 | 読んだJS **43本**（入口から辿れる物だけ） | 動かして数えた |
| ② | 同じ状態を2か所で持つ物 | この回で **1件 潰した**（テストが見本の会社名を書き写していた） | 押した |
| ③ | 法定 | 中央の表の kind **13**／給与の lib **35本**／出典は中央が唯一の正 | 動かして数えた |
| ④ | 「未対応」等の言葉 | **0件**（配る物6本・説明の書き込みは除く） | 動かして数えた＋押した |
| ⑤ | 黙って消える所 | **2通りを分けて出す**（下） | 動かして数えた＋押した |
| ⑥ | 人が埋める欄 | 畳んだまま **11**／ぜんぶ開く **14**／1人の詳細まで開くと **48** | 押して数えた |

### ④の中身（2026-08-19 に 6件 → 0件へ）
実物を読んだら、どれも「出来ていない」のではなく **読めていない／持っていない** だった。

| 前 | 何が起きているか | 今 |
|----|------------------|----|
| 「履歴保存が未対応です」×4 | 倉庫につながっていない（Store の口が無い） | 「過去の月を読み込めませんでした。ログインし直すと出ます。」**言葉は1か所**にまとめた |
| 「計算エンジン未対応」×1 | 年末調整の計算の部品が読めていない | 「年末調整の計算の部品が読み込めていません。画面を開き直してください。」 |
| 「対象年 …年は未対応のため」×1 | **その年の税額表を持っていない**（令和8・9年分だけ持っている） | 「対象年 …年の表は**持っていません**」 |

**確かめ方**
- 配る物6本のソース … **0件**（機械で数えた）
- 押して数えた … 辿り着けた画面では **0件**
- **未測定** … 賃金台帳／算定基礎届／月額変更届／労働保険 の4画面は
  **押して出す入口に辿り着けなかった**（描く口が外に出ていない）。**0件とは書かない。**

### ⑤の中身（2026-08-21 に 絞り込んだ）
道具＝`node scripts/silent-catch.mjs`（CIに載せた）。

**★2通りの数を 混ぜない★**（同じ物を2通りで数えたら、どちらの数字かを書く）
- **中身が空の `catch{}`** … **63件**（23本の中。お金の所に限らない）
- **catch が「空/0を返す」** … **26件**（23本の中・**お金の言葉で絞らない**）
  → **すべて 名前と理由を書いて残した**／**一覧に無い物 0件**（新しく出たら赤）


| 分け方 | 件数 | どうしたか |
|--------|------|-----------|
| catch ぜんぶ（23ファイル） | **203件** | 動かさず数えた |
| うち **お金・合計を作る所** | **82件** | 同上 |
| ①**0を返す** | **1件 → 0件** | **潰した** |
| ②空を返す | **9件 → 4件** | 5件は**潰した**／残り4件は**理由を書いて残した**（下） |
| ③そのまま進む | **38件** | 端末の記憶など。今は置く |
| ④知らせている | **3件** | 直した物＋「言ってから空を返す」物 |
| 外へ出す呼び出しの受け皿 | **3件 → 0件** | **潰した** |

**潰した1件**：Web明細の公開が失敗しても **0（＝0名 公開した）を返して黙っていた**。
呼び手は「今月を確定しました」とだけ出すので、**従業員に届いていない事が 誰にも伝わらない**。
→ **言って（画面に理由を出す）から 投げ直す**。呼び手も
「今月を確定しました。**従業員のWeb明細には公開できていません**」と分けて言う。

**潰した5件（②空を返す）**：端末に保存してある物を読む所（5対）。
壊れていても `[]` を返し、その `[]` を **そのまま上書き**していた＝**保存済みが黙って全部 消える**。
→ **使えない端末＝本当に空**（`[]` のまま）と **中身が壊れている＝読めなかった**（**書き込みを止める**）を分けた。

**残した4件（理由つき・増えたら赤）**
| 所 | なぜ残すか |
|----|-----------|
| `qrSvg` | QRの絵が作れない時。**お金ではない**（リンクは字でも出している） |
| `getStatutory` | 雲につながっていない時。**内蔵の表を使う**という設計 |
| `currentGensen` | 税が計算できない時。**0にしない＝null のまま**（紙に「未確認」と出る）／失敗は recalc が画面に言う |
| `currentCarry` | 同上（繰越）。**入金が読めていない時は null のまま**＝0にしない |

**確かめ方**：数は**動かして数えた**／失敗した時の画面は **実配信で押して確かめた**
（「今月を確定しました。従業員のWeb明細には公開できていません」が出た・幅も実測）。

---

## ★出す日にやる事（本番へ入れる日の手順）★

> ★ここは「状態」ではなく「その日にやる事」★。1行ずつ 押して消す。

1. ★勤怠CSVを取り込み直す★（2026-08-23 に受け口を直したため）
   - 直す前に取り込んだ月は ★時間外60時間超が 普通の残業(25%)のまま★＝★払い足りない★
   - ★取り込み直すと直る★（同じCSVをもう一度 取り込むだけ）
   - 本番は今 空なので ★今の客に実害は0★。★客が入る前に 1回やる★
2. 本番の倉庫の値を exally から写す（★記憶の値を打たない★）
3. 実配信を1回 開いて 刻印が今のコードと同じか見る

---

<!-- 数
{"読んだJS": 45,"会社の聞く問":7,"会社の持ち物": 36,"人の持ち物":62,"勤怠の日数":2,"検算_総支給":260000,"検算_控除":43730,"検算_手取り":216270,"検算_差":0,"紙の枚数":1,"紙の数字の個数":260,"紙_総支給":260000,"紙_手取り":216270,"配る口":1,"振込lib":1,"年末調整の口":1,"台帳の口":5,"出来ていない物の言葉":0,"何もしないcatch":46,"給与のlib": 36,"法定の出典kind":13,"欄_畳んだまま":10,"欄_ぜんぶ開く":15,"欄_1人の詳細まで":49,"動かして出たJSの落ち":0}
-->

**この数字が古くなったら CI が赤になります**（`node scripts/state-kyuyo.mjs --check`）。
直す時は 表の数字と 上の `<!-- 数 -->` の両方を、**測り直した数**にしてください。
