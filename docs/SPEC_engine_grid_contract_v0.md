# エンジン ↔ グリッド／チャット の契約 v0（設計・承認待ち）

作成 2026-08-02 ／ 対象 `exally-staging` ／ **コードは1行も書いていない**
前提 2026-07-31 の draft（[[project_exally_engine_grid_contract_v0_2026-07-31]]）を、**現物を読み直して**書き直した物。

---

## 0. 一行で

> **業務の答えを出すのは「オペレーション」1本だけ。グリッドもチャットも、それを呼ぶだけにする。**
> 数式で業務ロジックを書き直すことを、機械で禁止する。

---

## 1. まず現物（★2026-08-02 に実際にコードを読んで確認した）

**7/31の draft は前提が2つ古くなっていた。** 先にそこを直す。

| draft の記述 | 今の実物 |
|---|---|
| 「exally-formula.js は HyperFormula の plugin API を使わず…」 | **今は使っている**。第1〜3波で **43関数**をプラグインとして登録済み。`_jsSet`（外側だけで効く旧方式）は27個まで減り、入れ子で壊れる式は **5件**だけ |
| 「チャットは文字の数式を返すだけ＝契約が埋める穴」 | **穴はまだ開いている**（下記） |

### 既にある物（実装済み・テスト済み）

| 物 | 実体 | 状態 |
|---|---|---|
| 契約の型と検証 | `kyuyo/lib/op-contract.js` | ★実装済み。`defineOperation` が **「検証NGなら value を作らず errors を返す」を強制**している |
| 第一オペ | `kyuyo/ops/payroll.monthly.js` | ★実装済み。`id / version / title / desc / inputs / engine / law / excel / tests` を全部持っている |
| 堀（検証済み計算） | `kyuyo/lib/` の純関数群 | ★実装済み。`payroll-monthly.js` が月次給与の唯一の真実源 |
| テスト | `op-contract.test.js` / `ops-payroll-monthly.test.mjs` / `ops-golden-parity.test.mjs`（★1円一致）/ `law-switchpoints.test.mjs` | ★全部緑・CIで回っている |

### ★足りない物（これが契約 v0 の中身）

```
1. レジストリが無い    … defineOperation はあるが、id からオペを引く場所が無い
2. 誰も呼んでいない    … OpPayrollMonthly を呼ぶコードが repo 内にゼロ
                         （grep で確認。kyuyo の app.js すら lib を直接呼んでいて、オペ層を通っていない）
3. グリッドから呼べない … book.html に「オペを呼ぶ」入口が無い
4. チャットから呼べない … /api/claude は文字の数式を返すだけ
5. 「数式で再実装するな」を守る機械が無い
```

**＝オペ層は作ってあるが、宙に浮いている。** 契約 v0 は「宙に浮いた物を、2つの面に繋ぐ線」を決める物。

---

## 2. 呼び出しの型（何を渡して、何が返るか）

### 2.1 レジストリ（新設・1ファイル）

```js
// lib/op-registry.js
OpRegistry.register(op)      // op.id で登録。同じidの二重登録は投げる（黙って上書きしない）
OpRegistry.get('payroll.monthly')   // → op（無ければ null）
OpRegistry.list()            // → [{id,title,desc,version}]  ★チャットのカタログ／グリッドの関数一覧に使う
```

### 2.2 呼び出しの型（1つだけ。グリッドもチャットも同じ）

```js
const result = OpRegistry.get(id).engine(inputs)
```

**入力 `inputs`** … `op.inputs` の spec に従う素のオブジェクト。**セルでも自然文でもない**。
面（グリッド／チャット）が「自分の形 → inputs」に直してから渡す。★変換は面の責任。

**返り `result`** … 常にこの5つ。**成功でも失敗でも同じ形**（呼ぶ側が分岐を書き分けなくていい）。

| キー | 中身 | 決まり |
|---|---|---|
| `value` | 業務の答え（人ごとの明細と合計など） | ★検証NGなら **null**。0円や空を「成功」として返さない |
| `cells` | 表に置く形（AOA） | Excel出力と**同じ物**を使う＝画面とファイルがズレようがない |
| `warnings` | `[{code, level, scope, text, …}]` | 止めない。黄色い注意 |
| `errors` | `[{path, code, message}]` | `path` は `employees[3].fuyou` の形＝**どこが悪いか客に言える** |
| `provenance` | どのオペ・どの版・**どの年度の法定値が実際に選ばれたか** | ★「なぜその金額か」を後から言えるため |

### 2.3 面ごとの呼び方（両方とも同じレジストリ・同じengine）

```
グリッド  =PAYROLL.MONTHLY(会社設定, 従業員範囲, "2026-07")
            → book.html の関数が inputs に直す → engine → result.cells をセルへ展開
チャット  「7月の給料を出して」
            → /api/claude が【意図をオペidに解決】＋足りない入力を聞き返す
            → 同じ engine → result を見せる
            ★文字の数式を返さない。検証済みオペを呼ぶ。ここが穴の根治。
```

---

## 3. 責任の線（ここが契約の本体）

```
┌─ エンジン側（オペレーション） ─────────────────────────┐
│ ★業務の答えを出す唯一の場所                              │
│  ・金額・税・保険料・警告・法令の年度切替                 │
│  ・入力の検証（境界で弾く）                               │
│  ・result の5つを返す                                     │
│  ・headless（DOM・通信・乱数・現在時刻に触らない）        │
│  ★やらない: 画面に出す／ファイルを書く／セルを読む        │
└──────────────────────────────────────────────────────────┘
┌─ 面（グリッド／チャット）─────────────────────────────┐
│ ・自分の形 → inputs への変換                              │
│ ・result の見せ方（セルへ展開／文章にする／色を付ける）   │
│ ・ファイルの保存（XLSX.writeFile を呼ぶのは面）           │
│ ★やらない: 金額の計算。1円たりとも面で計算しない          │
└──────────────────────────────────────────────────────────┘
```

**境目の判定に迷ったら1つの問いで決める：**
> **その処理を消したら、答えの金額が変わるか？**
> 変わる → エンジン側。変わらない（見え方だけ変わる）→ 面。

---

## 4. `payroll.monthly` を、この型でどう呼ぶか（具体例1本）

**現物の記述子（`kyuyo/ops/payroll.monthly.js` から）**

```js
id: 'payroll.monthly'  version: '1.0.0'
title: '月次給与計算'
desc:  '1社1ヶ月の給与を全従業員ぶん計算し、明細・集計・Excel・法令の黄警告を返す'
inputs: { ym, company, employees[], … }   // 型・必須・enum を spec で持つ
law:    { basis, nendo, version }
excel:  { export(result) }                 // 純関数。writeFile は呼ばない
tests:  ['ops-payroll-monthly', 'ops-golden-parity', 'law-switchpoints', 'op-contract']
```

**グリッドから（新設する関数1つ）**

```
A1: =PAYROLL.MONTHLY(設定!A1:B20, 従業員!A1:Z50, "2026-07")
```

| 段 | 誰が | 何をする |
|---|---|---|
| ① | book.html の関数 | 範囲を読み、`{ym, company, employees[]}` に**詰め替えるだけ** |
| ② | `OpRegistry.get('payroll.monthly').engine(inputs)` | 計算。★ここだけが金額を決める |
| ③ | book.html | `result.errors` があれば **その場に赤で出す**（`path` からセルを特定）。無ければ `result.cells` を展開 |
| ④ | book.html | `result.warnings` はトーストで出す（止めない） |

**チャットから**

```
客: 「7月の給料を出して」
→ /api/claude: 意図 → id='payroll.monthly' に解決。inputs の required で足りない物を聞き返す
→ 同じ engine を呼ぶ
→ 「7月分、8人ぶん出しました。うち1人が最低賃金を下回っています（山田さん）」＋表
```

★**チャットが数式を書いて答えにしない。** オペを呼んだ結果だけを見せる。

---

## 5. ★「業務ロジックを数式で再実装しない」を機械で守る（型として）

**言うだけでは守られない。** 守りの型（今6つ）と同じやり方で、CIで止める。

### 守り方1：オペの外に金額の計算を書かせない
```
tests/op-boundary.test.mjs（新設）
 ・ops/*.js が lib 以外の計算を持っていないか（四則演算のべた書き・税率の直書きを検出）
 ・★面（book.html / chat.html）に、法定の数値（料率・控除額・最賃）が直書きされていないか
   → 見つけたら赤。「その数字は lib にある物を呼べ」と出す
```

### 守り方2：オペは tests が緑でないと公開できない
```
 ・op.tests に書いたテストが CI で回っているか（ci-coverage と同じ照合）
 ・回っていないテスト名が書いてあったら赤（嘘のゲートを許さない）
```

### 守り方3：同じ計算が2箇所に生えない
```
 ・既にある no-duplicate-libs（法定データは例外なし）をそのまま使う
 ・★オペが呼ぶ lib は kyuyo/lib/ の1本だけ、を型として足す
```

### 守り方4：`provenance` を必ず返す
```
 ・engine の返りに provenance が無ければ赤
 ・★「なぜその金額か」を後から言えない計算を、契約が受け付けない
```

---

## 6. 作らない物（v0では）

- HyperFormula の plugin API へオペを移植する（**不要**。オペは関数ではなく業務の単位）
- `kyuyo/lib/` の31本を一斉にオペ化する（**まず1本**。`payroll.monthly` が本物で1周するのを先に見る）
- 高度な自然文理解（**まずカタログから選ぶ＋足りない物を聞き返す**）
- 本番への反映（**stagingだけ**）

---

## 7. 承認をもらったら最初にやること（実装ではなく順番の提案）

1. `lib/op-registry.js`（レジストリ）＋ `op-boundary.test.mjs`（守り方1〜4）を**先に**作る ← テストツール先行
2. わざと壊して赤になるのを確認
3. `payroll.monthly` を登録し、**グリッドから1本呼ぶ**（`=PAYROLL.MONTHLY(...)`）
4. 真値は既存の `ops-golden-parity`（★1円一致）を流用＝**新しい真値は作らない**
5. 実配信の実ブラウザで確認 → CI全通し

**見積りは、承認をもらってから実物を読んで出す。** ここで数字を書くと当てずっぽうになる。

---

## 8. この設計で決めていない事（正直に）

- チャットが「意図 → オペid」を解決する方法（v0では**カタログから選ぶ**に倒す。NLUは後）
- オペが複数月・複数社をまたぐ時の形（v0は**1社1ヶ月**に固定）
- グリッドの `=PAYROLL.MONTHLY(...)` が**複数セルに展開**される時の見え方
  （★R15_no_spill：うちのグリッドはまだスピルの表示に未対応。`result.cells` の見せ方は P2 のグリッド作り直しと同時に決める）

---

## 9. 実装見積り（★2026-08-02・承認をもらってから実物を読んで出した）

指示役が足した2点を含む。**合計 8.0h（1日強）**。内訳と「何が緑になったら終わりか」を必ずセットで書く。

### 9.0 先に：測って分かった前提

| 測った物 | 結果 |
|---|---|
| `kyuyo/lib/op-contract.js` | 119行。`defineOperation` が **検証NG→`value:null`＋errors** を既に強制 |
| `kyuyo/ops/payroll.monthly.js` | 235行。`provenance` に `op/version/engines(13本)/law/statutory/watch` を**既に返している** |
| ★`provenance` に足りない物 | **出典URLと確認日**。`LAW.*.source` にURLはあるが、**実際に使った年度の値**に紐づいていない |
| ★中央 statutory の取り出し | `Store.getStatutory` の select が **`kind,year,data` だけ**。`source_url` / `verified_at` は**表にあるのに読んでいない** |
| ★内蔵値(オフライン時)の確認日 | **どこにも機械可読で無い**（libのコメントに文章であるだけ）→ 9.3の注意点 |
| 呼ぶ側の候補 | `kyuyo/js/app.js` の **`#b-xlsx`（給与明細Excel出力）**。今は `buildPeople(emps)` → `PayslipXlsx.download(people, opts)` |
| ★その候補が良い理由 | `download()` の中身は **`shukeiAOA` ＋ 人ごとの `meishiAOA`** ＝ **オペの `result.cells` と同じ物**。だから**新旧のAOAが完全一致するか**を機械で比べられる＝「通した」を証明できる |

### 9.1 内訳

| # | やる事 | 時間 | 備考 |
|---|---|---|---|
| 1 | `lib/op-registry.js`（register/get/list・二重登録は投げる）＋その単体テスト | **1.0h** | 新規1ファイル・小さい |
| 2 | `tests/op-boundary.test.mjs`（守り方1〜4） | **1.5h** | ★守り方1は**今日作った `no-hardcoded-statutory` をそのまま流用**、守り方3は `no-duplicate-libs` 流用。新規は「op.tests がCIで実際に回っているか」と「provenance を返さない engine を受け付けない」の2本 |
| 3 | ★(b) `provenance` に**出典URLと確認日** | **2.5h** | `Store.getStatutory` の select に2列追加／どのkind・yearをどの中央行で上書きしたかを保持／`provenance.statutory[kind]` に `{origin:'central'|'builtin', source_url, verified_at}`／中央あり・なしの両方でテスト |
| 4 | ★(a) **実際に1箇所から呼ぶ**（`#b-xlsx` をオペ経由へ） | **2.0h** | inputs詰め替え → `OpRegistry.get('payroll.monthly').engine(inputs)` → `result.cells.sheets` を `downloadSheets`。★新旧AOA完全一致テストつき |
| 5 | 実ブラウザ確認・再スタンプ・CI・報告 | **1.0h** | 実UIで `#b-xlsx` を実際に押してファイルが出る所まで |
|  | **合計** | **8.0h** | |

### 9.2 ★完了条件（これが全部緑になったら終わり。1つでも欠けたら未完）

1. `op-registry` の単体テストが緑（同じidの二重登録が**投げる**ことまで）
2. `op-boundary.test.mjs` が緑、かつ **わざと壊して赤になるのを実測**（self-test）
3. ★**新旧一致テスト**：`buildPeople→download` が作るAOA と `op.engine().cells` が**完全一致**（1セルでも違えば赤）
4. ★**provenance テスト**：中央から取れた時は `source_url` と `verified_at` が入る／取れない時は `origin:'builtin'` と分かる。**どちらの場合も空にならない**
5. ★**「呼ばれている」ことの機械確認**：`ops/*.js` の各オペが、テスト・ops配下**以外**の実コードから最低1箇所呼ばれていること。呼ばれていなければ赤。
   → **呼ばれていない契約を「有る」と言わない**を、今度は型にする（エンジン層が緑のまま誰も呼んでいなかった今の状態を、v0で繰り返さないため）
6. 既存が1つも赤くならない：`tests/run.js` 全緑／`ci-coverage` 緑／給与556緑／統合緑／**UI全ボタンで例外0**
7. 実ブラウザで `#b-xlsx` を押して Excel が出る・consoleエラー0

### 9.3 ★正直に言っておく不確実な所（見積りに含めた／含めていない）

- **含めた**：`buildPeople` と オペの `personOf` は別々に書かれている。金額は `ops-golden-parity` が1円一致を保証済みだが、**AOAの一致は誰も確かめていない**。差が出たら**寄せる作業が +1〜2h**（オペ側が正・app側を捨てる方向で寄せる）。
- **★含めていない（判断が要る）**：**内蔵値の「確認日」がどこにも記録されていない**。中央から取れた行は `verified_at` を返せるが、オフライン/中央未投入時に返す内蔵値は、確認日を**捏造できない**。
  v0では `verified_at: null, note:'内蔵値・確認日は未記録'` と**正直に返す**案にする。
  各libに確認日を1行入れて回るなら **+1.5h**（8本）。**どちらにするかは指示役の判断**。
- **含めていない**：グリッド `=PAYROLL.MONTHLY(...)` からの呼び出し。6章「作らない物」のとおり v0 の外。`result.cells` の見せ方は R15_no_spill と一緒に決める。

---

## 10. 実装（★2026-08-03・完了条件①〜⑧を全部緑にした）

### 10.1 入れた物

| 物 | 何をするか |
|---|---|
| `lib/op-registry.js`（新設） | 契約の入口。`OpRegistry.get(id).engine(inputs)` の1本。**同じidの二重登録は投げる**（黙って上書きすると provenance が嘘になる） |
| `kyuyo/lib/statutory-meta.js`（新設） | 内蔵値の**出典URL・確認日・指紋**。kind:year をキーに持つ |
| `kyuyo/js/app.js` | ★`#b-xlsx`（月次のExcel出力）が **registry → engine → cells** を通るようになった。面は詰め替えとファイル書き出しだけ。**検証NGならファイルを作らず、どこが悪いか（path）を出す** |
| `kyuyo/js/store.js` | `statutory` の select に **`source_url` / `verified_at`** を追加（表にあるのに読んでいなかった） |
| `kyuyo/ops/payroll.monthly.js` | `provenance.statutory.*` に **`origin`（central/builtin）・`source_url`・`verified_at`・`note`** を追加 |

### 10.2 守りの型（新しく5本・全部 self-test 付き）

| テスト | 見る物 |
|---|---|
| `tests/op-registry.test.mjs` | 入口の単体（★二重登録が投げるまで） |
| `tests/op-boundary.test.mjs` | 守り方1〜4 ＋ **⑤呼ばれているか** ＋ **⑧面を呼び返していないか** |
| `kyuyo/tests/statutory-freshness.test.mjs` | ★**指紋が合わなければ赤**＝率を変えたら確認日が自己失効する |
| `kyuyo/tests/ops-app-parity.test.mjs` | ★**今の app.js の道と オペの道が完全一致**（10データセット／69シート） |
| （流用）`no-hardcoded-statutory` / `no-duplicate-libs` | 守り方1・3。op-boundary が「まだ生きてCIで回っているか」を見る |

### 10.3 ★確認日は「実際に開いた物」だけ（捏造禁止）

13行のうち **3行に確認日を入れた**。残り10行は `verified_at:null` ＋ note に何が未確認かを書いた。

| kind:year | 確認日 | 実際に見た物 |
|---|---|---|
| `shakaihoken:2026` | 2026-08-03 | 協会けんぽ 令和8年度 都道府県別料率ページ。東京9.85/大阪10.13/北海道10.28/佐賀10.55/沖縄9.44・介護1.62%・支援金0.23% を突き合わせ。厚年18.3%固定・労使折半は日本年金機構で確認。★47県のうち5県を突き合わせた（残り42県は同じ一覧の表に拠る） |
| `koyo:2026` | 2026-08-03 | 厚労省(愛知労働局)の令和8年度 雇用保険料率。一般=労5/1000・事業主8.5/1000、建設=労6・事10.5、農林=労6・事9.5 |
| `shouhizei:2019` | 2026-08-03 | 国税庁 No.6101。標準10%・軽減8% |
| `saitei_chingin:2025` | **null** | 一覧ページは開き「令和7年度が現行」までは確認。★47県の金額はPDF側で機械で読めず、突き合わせていない |
| 所得税(月額/日額/電算)・賞与・年末調整・割増・過去年度 | **null** | ★今回開いていない |

### 10.4 ★1つだけ設計から動かした所（判断を仰ぎたい）

指示は「**各lib**に3点セットを持たせる」でしたが、**中央1本（`kyuyo/lib/statutory-meta.js`）に kind:year で持たせました。**

理由：指紋の元になる値の抜き出しを、既にある `buildStatutoryRows()`（中央 statutory へ投入する値を作る唯一の関数）に**そのまま使えるから**です。
各libに `rateSnapshot()` を10本手書きすると、**「その lib のどの欄が率か」を10回書くことになり、書き漏れた欄は指紋に入らず=変えても赤にならない**。
中央1本なら「中央へ送る値」と「指紋を取る値」が同じ1本になり、ズレようがありません。
**この判断が気に入らなければ各lib方式へ移します**（工数は同じ程度）。

### 10.5 ★実測（完了条件との対応）

| # | 完了条件 | 結果 |
|---|---|---|
| ① | registry 単体（二重登録が投げる） | **6緑** |
| ② | op-boundary＋わざと壊して赤 | **6緑 ＋ self-test 6緑** |
| ③ | 新旧AOA完全一致 | ★**10データセット／69シート 差分ゼロ** |
| ④ | provenance が中央/内蔵どちらでも空にならない | **統合テストで実測**（builtin=出典+確認日 or 理由／central=中央の値で上書き） |
| ⑤ | 呼ばれていることの機械確認 | **緑**（app.js から呼んでいる。外すと赤になるのを確認済み） |
| ⑥ | 既存が1つも赤くならない | run.js 全緑／給与556緑／統合39緑／**UI全ボタン58クリックで例外0**／ci-coverage 緑（CI 79本） |
| ⑦ | 実ブラウザで #b-xlsx が動く | ★**押して registry→engine→SheetJS まで到達**。ブック=「集計」＋人数ぶん、ファイル名 `給与明細_2026-08.xlsx`、**consoleエラー0** |
| ⑧ | オペが面を呼び返していない | **0件**（`../lib/` 以外の require と DOM 参照を赤にする） |

★指紋ガードは「率を1つ変えたら赤」を **雇用保険・健保1県・最賃1県の3通りで実測**（self-test 5緑）。

---

## 11. ★契約v0を本番へ出す条件（1行で決める・2026-08-03）

> **本番へ出す条件 ＝ 司さんが `#b-xlsx` を1回押し、出てきた明細のExcelを【PCで開いて】正しいことを目で確認したら。**
>
> ★2026-08-04 に書き直した（元は「実機（スマホ）で」）。理由は §11.1。

理由：契約経由に変えても客が受け取るファイルは1セルも変わらない（`ops-app-parity` が69シート完全一致で毎回見ている）。
機械で言えることは言い切っているので、残るのは **実機で1回開いて見る** だけ。
それが済んだら `exally`（本番）へ同じ内容を1コミットで反映する。**それ以外の追加条件は付けない**（付けると staging に溜まる）。


---

## 12. ★法定データの出典・確認日・指紋は「中央 statutory 表」が唯一の正（2026-08-03 決定）

> **人が直すのは中央 statutory だけ。lib 側の写し（`kyuyo/lib/statutory-central.generated.js` と `saitei-chingin.js` の47県）は `scripts/pull-statutory.mjs` が機械で作り直す。★lib を手で直さない。★**

理由：出典・確認日を lib にも手書きすると、中央と lib の**2箇所に手書きが残り、どちらを触っても片方が腐る**（＝確認日が静かに嘘になる）。
ズレていないことは `scripts/pull-statutory.mjs --check`（CI・中央と突き合わせ）と
`kyuyo/tests/statutory-freshness.test.mjs`（CI・ネット不要／指紋で突き合わせ）の2本で見張る。
lib が持つ「人の覚え書き（何をどこまで確かめたか）」だけは `statutory-meta.js` の `NOTES` に残す ―― **値でも日付でもないので中央と競合しない**。

### 12.1 発効日の書き方（★次に中央を読む人へ）

> **中央 statutory は発効日を【和暦】で持つ（例 `令和8年3月31日`）。一次情報の表記を落とさないため。
> 使う側が ISO（`2026-03-31`）へ直す。★自前の変換を書かない。★ `SAITEI_CHINGIN.toIsoHatsuko` / `toWarekiHatsuko` の1箇所だけを使う。**

理由：日付の判定（発効前か後か）は文字列比較でやるので ISO でないと壊れる。
変換が2箇所以上に生えると、そこからドリフトが生まれる（和暦の元号加算をどこかで間違える）。
両方向は `kyuyo/tests/integration.mjs`（中央の形で hydrate して ISO で入ることを実測）で固定してある。

### 12.2 ★出典URLの死活検査は「誰が見るか」まで決める（2026-08-04）

> **週1（月曜9時JST）の `source-urls` ワークフローが落ちたら、その週のうちに実装セッションが気づき、
> ★次の報告に必ず「出典URL 生きているN／死んでいるM」の1行を載せる★。死んでいたら、生きている一次情報を探して
> 指示役に出す（中央への書き込みは指示役）。**

理由：**誰も見ない週1は、無いのと同じ**。GitHub Actions の失敗通知はコミット作者にしか飛ばず、
定期実行では埋もれる。だから「機械が落ちる」だけでなく **「人がどこで受け取るか」** を決めておく。
報告に毎回1行入れる形にしたのは、**落ちていない週も数字が出る**＝止まっていること自体に気づけるため
（ワークフローが壊れて動いていない、が一番危ない）。

補足：`warimashi:2023` の出典は和歌山労働局のページ。**都道府県の労働局なので全国一律の法定率の出典としては本来弱い**が、
4つの率（2割5分・3割5分・5割・月60時間）が1ページに揃っている `mhlw.go.jp` 配下のページが他に無いことを確認したうえで採用した。
**消えたらこの週1検査が拾う**（本体に無かったことを無理に埋めない）。

### 11.1 なぜ「PCで開いて目で見る」に書き直したか（2026-08-04）

見てほしいのは **中身（金額・シート構成）が正しいか** であって、**スマホでファイルを受け取れるか** ではない。
この2つを1つの条件に混ぜていたため、**スマホ側の不具合（種類がoctet-stream・共有シート無し）で
中身の確認まで止まってしまった**。分ける：

| 何を確かめるか | どこで | 誰が |
|---|---|---|
| 中身が正しい（金額・シート） | **PCで開く** | 司さん（1回） |
| ファイルを受け取れる（共有シート・Excelで開く） | iPhone / Android の実機 | 司さん（各1回） |

★**司さんに実機で触ってもらうのは、実配信に乗っていることを測って確認した後だけ**（2026-08-04 に2回、
古い画面を触らせてしまった）。報告の1行目に「push済みか・実配信に乗っているか」を必ず書く。
