/* seikyu-templates.js — ★様式（テンプレ）＝見た目と初期の列だけ★
 * ==============================================================================
 * 代行請求アプリと同じ考え方（invoice-pdf.js:324/338 の pdfDesign = classic / elegant）。
 *   classic … 緑の帯で罫線のしっかりした、いつもの請求書
 *   elegant … 罫線を減らして字で見せる、落ち着いた請求書
 *
 * ★テンプレが決めてよいのは「見た目」と「最初に並ぶ列」だけ★
 *   ・金額・消費税・合計は ★1円もテンプレに依らない★（totalsOf がそれを守る）
 *   ・列は最初の並びを配るだけ。あとは会社が足す・消す・幅を変える
 *
 * ★既定の7列は、ここに置いた「std1 の初期値」であって、紙に焼き付いた物ではない★
 *
 * 【利用】ブラウザ window.SeikyuTemplates ／ Node require('./seikyu-templates.js')
 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory(require('./seikyu-tax.js'), require('./seikyu-cols.js'));
  } else {
    root.SeikyuTemplates = factory(root.SeikyuTax, root.SeikyuCols);
  }
})(typeof self !== 'undefined' ? self : this, function (TAX, COLS) {
  'use strict';
  if (!TAX || !TAX.compute) throw new Error('seikyu-tax.js を先に読んでください');
  if (!COLS) throw new Error('seikyu-cols.js を先に読んでください');

  /* 色は★直hex★（このリポジトリの現行ルール）。緑は #2E7D54 だけ（濃い緑の禁止色は使わない）。 */
  var TEMPLATES = {
    std1: {
      id: 'std1',
      /* ★名前は「何が違うか」を言う★（司さん 2026-08-31「いつものってなんど」）
         ＝気分の言葉（いつもの／すっきり）では 選べない。★紙の中身★で 名前を付ける。 */
      label: '品名・数量・単価（罫線あり）',
      // ★人に見せる字に 別の製品の名前（内部の言葉）を出さない★（司さん 2026-08-16 ⑥）
      note: '品名・数量・単位・単価・金額・消費税の表。見出しに帯、明細に罫線。'
        + 'いちばん よく使う形です。',
      cols: {
        items: ['#', '品名・内容', '数量', '単位', '単価', '金額', '消費税'],
        widths: { '#': 28, '品名・内容': 220, '数量': 56, '単位': 44, '単価': 80, '金額': 100, '消費税': 72 },
        aligns: {},
      },
      theme: {
        /* ★読ませる字は薄い黒／線は1種類★（司さん 2026-08-16・見本＝代行請求 invoice-pdf.js） */
        ink: '#333333',       // 本文・数字・金額
        sub: '#6B6B6B',       // 補足
        line: '#B0B0B0',      // 罫線（★白黒/FAXで消えない濃さ★）
        accent: '#B0B0B0',    // 飾り線も同じ（色で強弱を作らない）
        band: '#F0FAF4',      // 帯の地
        headBg: '#F2F2F2',    // 表の見出しの地（無彩色の面）
        headInk: '#333333',
        grandInk: '#333333',
        rule: 'rows',         // 罫線 = 横線だけ（うちの紙は縦罫を引かない）
        titleSpacing: '.32em',
        grandGo: 'ご',        // 「ご請求金額（税込）」＝ classic系の言い方（invoice-pdf.js:957）
        /* ★★2026-09-08 memoBox は false に した★★
           司さん 2026-09-05「他2つは おれの様式のように デフォで 備考欄つけとけよ」を
           ★足元の 箱★と 読み違えていた。実物を 見せてもらって 分かったのは
           ★「備考」は 明細の 列★（消費税の 右隣・行ごとに 現場名を 書く）。
           ⇒ 足元の 箱は 出さない。備考は ★設定 ▸ 明細の列★で 足す
             （役目は seikyu-cols.js に 前から 在る＝'備考'／'摘要' → memo）。
           ★決まりごと そのものは 残す★＝会社が 足元にも 欲しい と 言った日に すぐ 戻せる。 */
        memoBox: false,
        /* ★2026-09-08 司さん「他のテンプレもできることはやって見せて（項目の合計など）」★
           ＝控除を 入れると この様式でも 塊が 3つに なる（項目／控除／差引）。
             ★前は ①の合計は 出るのに ③の 見出しだけ 出ていなかった★（実測）。
           ★地の色は 様式なりの物★＝この様式の headBg を そのまま 使う
             （elegant は わざと 白＝罫線ひかえめの 性格を 壊さない）。 */
        itemSum: '項目の合計',
        sumsHead: '差引請求額',
        /* ★控除が 無い 紙の 3つ目の 帯★（2026-09-08 司さん
           「この赤の線にも 控除ありの時のように 分かりやすくやって」）
           ★「差引」は 使えない★＝引く物が 無いので 嘘に なる。
           ★2026-09-08 司さん決定＝「合計金額」★（私の 既定「ご請求金額」を 差し替え）
           ★調べて 分かった事★
             ・★「ご請求金額」は 紙の 頭の 金額の 呼び名★＝
               うちの 紙も 頭に 出しているので ★同じ言葉が 2回★ 出ていた（司さんの 赤丸）。
             ・請求書ソフト board の 言い方＝「合計欄／合計金額欄」は ★ヘッダー部分★、
               明細の 下は「明細下の合計行（小計・消費税・合計の3行）」で
               ★この3行に 名前は 付いていない★＝★世の中に この見出し自体が 無い★。
             ・建設業の 請求書に 出てくる 語のうち ここに 合うのは ★合計金額★。
           ★控除ありの「差引請求額」と 対に なる★
             控除あり … 引いた 後の 請求額／控除なし … 足した 後の 合計 */
        sumsHeadPlain: '合計金額',
      },
    },
    elegant: {
      id: 'elegant',
      label: '品名・数量・単価（罫線ひかえめ）',
      note: '列は上と同じ。罫線を減らして、字の大きさと余白で読ませる形です。',
      cols: {
        items: ['#', '品名・内容', '数量', '単位', '単価', '金額', '消費税'],
        widths: { '#': 28, '品名・内容': 240, '数量': 56, '単位': 44, '単価': 84, '金額': 108, '消費税': 72 },
        aligns: {},
      },
      theme: {
        ink: '#333333',
        sub: '#7A7A7A',       // すっきり＝補助文をもう一段 薄く
        line: '#C4C4C4',      // すっきり＝罫をもう一段 薄く（消えない範囲で）
        accent: '#C4C4C4',
        band: '#FFFFFF',
        headBg: '#FFFFFF',
        headInk: '#333333',
        grandInk: '#333333',
        rule: 'rows',         // 罫線 = 横線だけ
        titleSpacing: '.5em',
        grandGo: '御',        // 「御請求金額（税込）」＝ elegant系の言い方（invoice-pdf.js:479）
        /* ★★2026-09-08 memoBox は false に した★★
           司さん 2026-09-05「他2つは おれの様式のように デフォで 備考欄つけとけよ」を
           ★足元の 箱★と 読み違えていた。実物を 見せてもらって 分かったのは
           ★「備考」は 明細の 列★（消費税の 右隣・行ごとに 現場名を 書く）。
           ⇒ 足元の 箱は 出さない。備考は ★設定 ▸ 明細の列★で 足す
             （役目は seikyu-cols.js に 前から 在る＝'備考'／'摘要' → memo）。
           ★決まりごと そのものは 残す★＝会社が 足元にも 欲しい と 言った日に すぐ 戻せる。 */
        memoBox: false,
        /* ★締めの 見出し（合計金額）だけ 地を 入れる★（2026-09-09 司さん
           「なんで②だけ 下の合計金額のタブに 背景いれて 合わせてないんど」）
           ★実測★ この様式は headBg が #FFFFFF（地を 使わないのが 売り）なので、
             ★下の 締めの 帯だけ 地が 抜けて★ ほかの 様式と 揃っていなかった。
           ★明細の 見出しは 白のまま★＝言われたのは 下の 締めの 帯だけ。 */
        sumsBg: '#F2F2F2',
        /* ★2026-09-08 司さん「他のテンプレもできることはやって見せて（項目の合計など）」★
           ＝控除を 入れると この様式でも 塊が 3つに なる（項目／控除／差引）。
             ★前は ①の合計は 出るのに ③の 見出しだけ 出ていなかった★（実測）。
           ★地の色は 様式なりの物★＝この様式の headBg を そのまま 使う
             （elegant は わざと 白＝罫線ひかえめの 性格を 壊さない）。 */
        itemSum: '項目の合計',
        sumsHead: '差引請求額',
        /* ★控除が 無い 紙の 3つ目の 帯★（2026-09-08 司さん
           「この赤の線にも 控除ありの時のように 分かりやすくやって」）
           ★「差引」は 使えない★＝引く物が 無いので 嘘に なる。
           ★2026-09-08 司さん決定＝「合計金額」★（私の 既定「ご請求金額」を 差し替え）
           ★調べて 分かった事★
             ・★「ご請求金額」は 紙の 頭の 金額の 呼び名★＝
               うちの 紙も 頭に 出しているので ★同じ言葉が 2回★ 出ていた（司さんの 赤丸）。
             ・請求書ソフト board の 言い方＝「合計欄／合計金額欄」は ★ヘッダー部分★、
               明細の 下は「明細下の合計行（小計・消費税・合計の3行）」で
               ★この3行に 名前は 付いていない★＝★世の中に この見出し自体が 無い★。
             ・建設業の 請求書に 出てくる 語のうち ここに 合うのは ★合計金額★。
           ★控除ありの「差引請求額」と 対に なる★
             控除あり … 引いた 後の 請求額／控除なし … 足した 後の 合計 */
        sumsHeadPlain: '合計金額',
      },
    },
    /* ★3つ目＝工事代金＋控除（差引で出す）★（指示役の裁定 2026-08-27）
       ─────────────────────────────────────────────────────────
       ★実物 47通のうち 11通（4分の1）がこの形★（八木工業10通＋ENEOS 25.3 の2ページ目）。
       ★std1/elegant に足さず 3つ目として作る★＝★表の列数から違う（2列）★ので、
       既存を条件分岐で汚すと ★両方 壊れる★。

       ★計算には1文字も触っていません★＝控除の仕組みは seikyu-doc.js に もう在る
       （deductionsOf／deductTotalOf／deductLines／恒等式 請求額＝（税抜＋値引き）＋消費税−控除）。
       ここが足すのは ★出し方だけ★＝2列の表／控除明細の呼び名／中計の呼び名／振込先を1行に。

       ★実物（八木工業 2025/9）★
         項目            金額            ← 2列だけ
         工事代金       197,600
         消費税          19,760
         小計           217,360          ← 税込の小計
         控除明細                        ← 見出しだけの行
         弁当代 矢原      7,310
         健康診断代      10,164
         中計            17,474          ← 控除の合計（★4通は「小計」と呼ぶ＝呼び名は会社が決める★）
         合計           199,886          ← 小計 − 中計
         振込先（1行だけ）／対象期間 2025/8/21 ~ 2025/9/20 */
    koujo: {
      id: 'koujo',
      /* ★別の製品の名前を 画面に出さない★（screen-words が 赤にする）
         ＝「工事・運転の請求」と 仕事の名前で 言う。 */
      label: '項目と金額だけ＋控除（工事・運転むけ）',
      note: '数量・単価を出さず、項目と金額だけの表。控除（弁当代など）を引いて'
        + '「請求額」を出します。対象期間も入れられます。',
      cols: {
        items: ['項目', '金額'],
        widths: { '項目': 340, '金額': 140 },
        aligns: {},
      },
      theme: {
        ink: '#333333',
        sub: '#6E6E6E',
        line: '#B7B7B7',
        accent: '#2E7D54',
        band: '#FFFFFF',
        headBg: '#F1F7F4',
        headInk: '#333333',
        grandInk: '#333333',
        rule: 'rows',
        titleSpacing: '.4em',
        grandGo: '御',
        /* ★呼び名は焼き付けない★＝会社ごとに変えられる（既定はこの2つ・指示役の裁定） */
        dedHead: '控除明細',
        /* ★2026-09-09 司さん「1個目の赤丸のところ 中計は ★控除の合計★」★
           ＝この 塊は 控除明細（弁当代など）の 締め。
             「中計」は ★締めの 途中の 計★（合計−控除の 手前）に 使う 字に した。 */
        dedSum: '控除の合計',
        /* ★2026-09-08 司さん「赤丸の所に項目の合計は？」
             「3番目の青線の所に なんの塊か 上の2つと分かるように 何が作れや」★
           ＝控除を出す紙は ★塊が3つ★（項目／控除明細／差引請求額）。
             上の2つには 見出しと 合計が 在るのに、
             ★①には 合計が 無く ③には 見出しが 無かった★。
           ★呼び名は ここが 持つ★（会社ごとに 変えられる・焼き付けない）。 */
        itemSum: '項目の合計',
        /* ★2026-09-08 司さん決定＝「差引請求額」★（私の「請求額の計算」を 差し替え）
           ★調べた事★
             ・うちの実物（八木工業 2025/9）には ★3つ目の塊の 見出しが 無い★
               ＝小計・消費税は 項目の表の中／控除の後は「合計」1行だけ。
               ⇒「実物では こう」という 答えが 出せない所だった。
             ・世の中の 請求書で この位置に 使う 言葉＝
               ★差引請求金額／差引請求額★（建設業）／差引支払額／今回請求金額／請求金額の内訳
             ・「請求額の計算」は ★1件も 出なかった★＝私が 作った 言葉だった。
             ・うちの 給与明細は「支給／控除／★差引支給額★」＝身内で 言い方が そろう。 */
        sumsHead: '差引請求額',
        /* ★控除が 無い 紙の 3つ目の 帯★（2026-09-08 司さん
           「この赤の線にも 控除ありの時のように 分かりやすくやって」）
           ★「差引」は 使えない★＝引く物が 無いので 嘘に なる。
           ★2026-09-08 司さん決定＝「合計金額」★（私の 既定「ご請求金額」を 差し替え）
           ★調べて 分かった事★
             ・★「ご請求金額」は 紙の 頭の 金額の 呼び名★＝
               うちの 紙も 頭に 出しているので ★同じ言葉が 2回★ 出ていた（司さんの 赤丸）。
             ・請求書ソフト board の 言い方＝「合計欄／合計金額欄」は ★ヘッダー部分★、
               明細の 下は「明細下の合計行（小計・消費税・合計の3行）」で
               ★この3行に 名前は 付いていない★＝★世の中に この見出し自体が 無い★。
             ・建設業の 請求書に 出てくる 語のうち ここに 合うのは ★合計金額★。
           ★控除ありの「差引請求額」と 対に なる★
             控除あり … 引いた 後の 請求額／控除なし … 足した 後の 合計 */
        sumsHeadPlain: '合計金額',
        /* ★振込先は1行★（実物11通とも1行） */
        bankOneLine: true,
      },
    },

    /* ★5つ目＝司さんの 実物の 形★（2026-09-08 司さん
       「ついでに おれが言うてる 項目の右端が 備考欄になる タイプも テンプレ化してたら 良くない？」
        →「おれのバージョンのも テンプレ化したら？」）
       ★実物★ 黒田空調 2026/7分（司さんが 見せてくれた PDF）
         項目／数量／単位／金額／消費税／★備考★
         備考に ★現場名★（東予市 川本邸・菊水ホテル・鳥越ハンカチ…）を 1行ずつ
         ★単価の 列が 無い★／消費税は 行ごとに 出る／足元は 振込先＋小計・消費税・合計
       ★「備考」は 前から 役目を 持っている★（seikyu-cols.js＝'備考'／'摘要' → memo）
         ＝値は 明細の 行の memo。ここは ★列に 並べただけ★。 */
    genba: {
      id: 'genba',
      label: '項目・金額＋備考（現場名を書く）',
      note: '単価の列を 出さず、いちばん右に 備考の列を 出します。'
        + '行ごとに 現場名などを 書けます（工事・設備むけ）。',
      cols: {
        items: ['項目', '数量', '単位', '金額', '消費税', '備考'],
        /* ★備考を 広げた★（2026-09-09 司さん「備考欄の幅をもう少し広げて」）
           160 → ★210★（+50）。減らしたのは ★項目★ 190→150（-40）と
           ★消費税★ 72→62（-10）＝合計は 前と 同じ（600px）。
           ★現場名は 長い★（実物「東予市 川本邸」「鳥越ハンカチ」）ので
           備考が 折り返さない事を 優先した。 */
        /* ★2026-09-10★ 消費税の 右に すきま（5mm）を 入れたので、
           ★消費税の 幅を 広げる★＝数字が 詰まらない（62→84）。
           減らしたのは 項目 150→142 と 備考 210→200（合計は 前と 同じ 600）。 */
        /* ★2026-09-10 司さん「これを1塊として もう少し 近づけて バランスとって
             項目と備考は 多めに」（赤丸＝数量・単位・金額・消費税）★
           ＝真ん中の 4列を ★1つの 塊★として 詰め、空いた分を ★項目と 備考★へ 回す。
             塊 262→222（-40）／項目 142→162・備考 196→216（+20 ずつ）
           ★表の 横幅は 前と 同じ 600★（列の 割り振りだけ 変えた）。
           ★数字が 折り返さない事を 測ってから 決めた★（scripts/shot-koujo-genba.mjs）。 */
        widths: { '項目': 176, '数量': 34, '単位': 28, '金額': 68, '消費税': 66, '備考': 228 },
        aligns: {},
      },
      theme: {
        ink: '#333333', sub: '#6B6B6B', line: '#B0B0B0', accent: '#B0B0B0',
        band: '#FFFFFF', headBg: '#F2F2F2', headInk: '#333333', grandInk: '#333333',
        rule: 'rows', titleSpacing: '.32em', grandGo: 'ご',
        memoBox: false,
        itemSum: '項目の合計',
        sumsHead: '差引請求額',
        sumsHeadPlain: '合計金額',
      },
    },
  };

  /* ★★様式は「備考あり／なし」で 対に する★★（2026-09-10 司さん
       「テンプレで それぞれ ★あり版 なし版★で 並べるんやろが」
        →「おれが いよる あり版なし版は ★備考★の ことやろが」）
     ＝前は 備考の 列を 持つ様式が genba 1つだけで、
       「罫線ありの 紙に 現場名も 書きたい」「控除も 消費税も 備考も 要る」が 選べなかった。
     ★相方は 元から 機械で 生やす★＝元を 直せば 両方 直る（★片方だけ 直る 事故を 止める★）。
     ★列の 割り振りは 様式ごとに 明示★＝
       司さんの 決め（2026-09-10）「真ん中の 4列は 1塊に 詰めて ★項目と 備考は 多めに★」。
     ★前からの id は 1文字も 変えない★＝出した紙の 様式が 迷子に ならない。 */
  function kaeHan(motoId, id, label, note, items, widths) {
    var t = JSON.parse(JSON.stringify(TEMPLATES[motoId]));
    t.id = id; t.label = label; t.note = note;
    t.cols = { items: items, widths: widths, aligns: {} };
    return t;
  }
  TEMPLATES.std1memo = kaeHan('std1', 'std1memo',
    '品名・数量・単価（罫線あり）＋備考',
    'いちばん上と 同じ形に、いちばん右へ 備考の列を 足した物。行ごとに 現場名などを 書けます。',
    ['#', '品名・内容', '数量', '単位', '単価', '金額', '消費税', '備考'],
    { '#': 22, '品名・内容': 158, '数量': 34, '単位': 28, '単価': 56, '金額': 62, '消費税': 66, '備考': 174 });
  TEMPLATES.koujomemo = kaeHan('koujo', 'koujomemo',
    '項目・金額＋控除＋備考（現場名を書く）',
    '控除（弁当代など）を 引く紙に、行ごとの 消費税と 備考の列を 足した物。',
    ['項目', '数量', '単位', '金額', '消費税', '備考'],
    { '項目': 176, '数量': 34, '単位': 28, '金額': 68, '消費税': 66, '備考': 228 });
  TEMPLATES.genbanashi = kaeHan('genba', 'genbanashi',
    '項目・金額（単価を出さない）',
    '単価の列を 出さず、項目・数量・単位・金額・消費税だけ。備考は 出しません。',
    ['項目', '数量', '単位', '金額', '消費税'],
    { '項目': 404, '数量': 34, '単位': 28, '金額': 68, '消費税': 66 });

  /* ★並びは「なし → あり」の 対★＝対が 離れて 見えないように 順を 決める。
     ★ここに 無い id は 後ろに 付ける★＝様式を 足した日に 一覧から 消えない。 */
  var NARABI = ['std1', 'std1memo', 'koujo', 'koujomemo', 'genbanashi', 'genba'];
  /* ★選べる 一覧から 隠す★（2026-09-10 司さん「③④ いらんことないか？」）
     ＝「罫線ひかえめ」は ★罫線の 濃さだけ★が 違う 別物で、
       選ぶ人から 見ると 上の2つと 見分けが つかない。
     ★中身は 消さない★＝倉庫に template_id='elegant' で 保存された 紙が 在ると、
       消した瞬間に ★出した紙の 顔が 変わる★（getOrDefault が 既定へ 落とす）。
       ⇒ 選択肢から 外すだけ。出した紙は 今までどおり その顔で 出る。
     ★今日 作った elegantmemo は まるごと 消した★＝誰も まだ 使っていない。 */
  var KAKUSU = ['elegant'];

  var DEFAULT_ID = 'std1';

  function get(id) {
    var t = TEMPLATES[String(id || '')] || null;
    return t ? clone(t) : null;
  }
  function getOrDefault(id) { return get(id) || get(DEFAULT_ID); }
  /** ★この様式は 控除を 出す紙か★（司さん 2026-09-05「今 控除ないやつ 選んどんのに 控除があるし」）
   *  ＝控除明細の 見出しを 持っている様式＝控除を 出す前提の 紙。
   *  ★紙そのものは どの様式でも 控除を 出せる★（seikyu-paper.js:336 は 控除の行が 在るかで 決める）。
   *  ここが 答えるのは ★設定の画面に 控除の欄を 出すか★だけ。 */
  function usesDeduction(id) {
    var t = get(id) || get(DEFAULT_ID);
    return !!(t && t.theme && t.theme.dedHead);
  }
  function list() {
    var keys = Object.keys(TEMPLATES);
    var jun = NARABI.filter(function (k) { return keys.indexOf(k) >= 0; })
      .concat(keys.filter(function (k) {
        return NARABI.indexOf(k) < 0 && KAKUSU.indexOf(k) < 0;
      }));
    return jun.map(function (k) {
      var t = TEMPLATES[k];
      return { id: t.id, label: t.label, note: t.note };
    });
  }
  function clone(v) { return JSON.parse(JSON.stringify(v)); }

  /** その1通で使う列。会社が決めた物（inv.data.cols）が最優先、無ければテンプレの初期値。 */
  function colsOf(inv) {
    var d = (inv && inv.data) || {};
    var own = d.cols;
    if (own && Array.isArray(own.items) && own.items.length) return COLS.normalizeSpec(own);
    return COLS.normalizeSpec(getOrDefault(inv && inv.template_id).cols);
  }

  /**
   * ★金額は様式に依らない★
   *   ここは template_id を1度も見ない。見た瞬間に「見た目で金額が変わる」バグになる。
   *   totalsOf({ inv, lines }) → seikyu-tax.compute の返り
   */
  function totalsOf(o) {
    o = o || {};
    var inv = o.inv || {};
    return TAX.compute({
      lines: o.lines || inv.lines || [],
      taxMode: inv.tax_mode,
      rounding: inv.rounding,
    });
  }

  return {
    TEMPLATES: TEMPLATES, DEFAULT_ID: DEFAULT_ID,
    get: get, getOrDefault: getOrDefault, list: list,
    colsOf: colsOf, totalsOf: totalsOf, usesDeduction: usesDeduction,
  };
});
