# ★★★ 絶対のルール（全アプリ共通・2026-08-05 司さん指示）★★★

## 1. テストで確かめてから本番へ。順番を飛ばさない

**テスト環境は「本番に出す前にバグを見つける場所」。飛ばしたら存在する意味がない。**
★ユーザーが付いてからこれをやると、客の画面が壊れる★

- 直す → **テスト側で直して、テストの配信で確かめる** → **本番へ1コミット** → **本番の配信で確かめる**
- 本番でしか起きない事情がある時だけ例外。その時は★「なぜテストを飛ばすか」を先に書いて指示役に出す★。**黙って飛ばさない**
- ★**お金・法令・客のデータ・外部への書き込み（振込／広告配信／請求／メール送信）に触る変更は、例外を認めない**★

## 2. 片方に入れたら、その場でもう片方の扱いを決める

「本番だけでよい」「テストだけでよい」なら**理由を書く**。書けないなら**両方に入れる**。
★**決めずに次の作業へ進まない**★

**実際に起きた事故（2026-08-05）**
- Exally: これを守らず★**差が65件**★まで開き、★**グリッドのExcel保存が上書きで消える寸前**★だった。
  「日付が新しいほうを正」と機械的に決めていたら、機能が1つ消えていた。
- アマかせ: ★**テストにあって本番に無い物が0件**＝テストを1回も通していなかった★。
  「広告費の二重計上」「勝手な広告再開」の修正が本番直行していた。

## 3. 差を毎回数える

- **報告の1行目**に ★「push済みか／CIが緑か／実配信に乗っているか」＋「差 N件」★ を必ず書く
- 差を数える道具は★**両方のリポジトリに置く**★（片方だけに入れて終わらない）
- 比べる前に★**改行コード(CRLF/LF)と `?v=` の刻印をそろえる**★。そろえないと本物の差が見かけの差に埋まる
- 片側にしか無くてよい物は★**理由つきの一覧**★。理由が書けない差は許さない
- ★**差が10件を超えたら新しい作業に入らない**★
- 合わせる時、★**「日付が新しいほう」で機械的に決めない**★。中身を1本ずつ見る
- ★**入れ物が違う所は合わせない**★（倉庫の接続先・配信先のパス・PWAの入口など）。**理由を書いて一覧に載せる**

## 4. 「pushした」＝「直しが入った」ではない

設定を間違えると★**画面は正常に見えたまま、直しだけが入らない**★。

- 塞ぐ・移す・切り替える作業は、★**「本当にそうなったか」を叩く見張りを先に作る**★
- ★**実配信の実物を測ってから「乗った」と書く**★（repoに入っている＝配信に乗っている、ではない）
- 設定ファイル（`vercel.json` / `manifest.json` / `package.json` 等）に★**説明文やメモを書かない**★。
  規格外の項目が1つ混ざるだけで**組み立て前に失敗する**（ダイコメ 8/2・Exally 8/5 で実際に発生）

## 5. 作る前に、社内に同じ物が無いか探す

★**外から仕組みを持ってくる前に、動いている実物を探す**★

- 2026-08-04: iPhoneでExcelが開けない件で「共有シートを出す」を作ったが、
  ★**代行請求アプリに正しいやり方が既に動いていた**★（正しい種類 + `a.download`）。作り直しだった。
- 2026-08-05: 入口の見張りを★**2回作った**★（テスト側と本番側で別々に）。

## 6. テストは実物と同じ形で

- ★**実機と違う形のテストは嘘をつく**★（`load()` を呼ばない／デスクトップだけ／作り物のfixtureだけ）
- ★**SKIP を緑と呼ばない**★。道具が無い日に黙って緑になる形は赤にする
- ★**空振りしているテストは、無いより悪い**★（守っているつもりになる）。
  読み取りが0件/nullなのに緑になっていないか、必ず確かめる
- ★**わざと壊して赤になることを実測してから閉じる**★


## 7. ★★コードを触る時の順番（全アプリ共通 HARD RULE・毎回・飛ばさない）★★

**①テストの道具を作る → ②テストで確かめる → ★③Claude Code が自分で実際に操作して確かめる★ → ④司さんに報告**

★このブロックは全アプリの CLAUDE.md に置く（新しく作るアプリにも必ず入れる）★
（司さん指示 2026-07-14「これから作るアプリもやから GitHub の気づく所に記憶しといて」）

### ① テストの道具が先（後付けにしない）
- 直す前に「どう測るか」を先に作る。★直してから測り方を考えると、直した物に都合のいい測り方になる★
- ★自分で書いたテストは、勘違いごと固定する★。あるべき挙動を強制する形にする
- ★わざと壊して赤になることを実測してから「緑」と呼ぶ★

### ② 全機能を実データで確かめる（計算・ロジック層）
- そのアプリの**本物のエンジン**に**全パターン（全入力の組合せ）**を流し、**期待値と assert で突き合わせる**
- 網羅・invariant・NaN/矛盾ゼロ・法定値は一次情報照合。★中間値でなく、ユーザーに見える出力で判定する★

### ③ ★Claude Code が実UIで全ボタン・全パターンを操作する★
- **人間より速く正確にできる Claude Code の強み**。実アプリを Playwright/jsdom で動かし、
  **全タブ・全ボタン・全入力**を実際に押す/打ち込む
- ★「repoに入った」「配信のバイトに入っている」「テストが緑」は、どれも操作したことにならない★
- 確認する: **JSエラー0**・各画面が描画・入力で正しく再計算・**出力(PDF/Excel/振込データ)の実生成**まで
  （＝配線が本当に生きているか）。破壊/DL/ダイアログ系はデナイリストで安全に
- ログインが要る画面は★テスト環境に自分のアカウントを作って通す★（本番で他人の垢を使わない）
- ★押していないなら「未測定」と書く★。0件・異常なしにしない

### ④ 報告
- ★報告の1行目★＝ push済みか／CIが緑か／実配信に乗っているか／★実際に操作したか★
- ★ローカル緑は CI緑ではない★。CIの結果を数字で出す
- ★見た目を変えたら、実アプリの実スクショを司さんに見せて、OKをもらうまで push しない★
  （作り物の見本ではなく★配信された本物の画面★を撮る）
- ★「スクショを撮った」「幅を390/360にして見た」も 操作したことにならない★
  （撮影・バイト・CI緑は ★測定★ であって ★操作★ ではない）
- ★見せる前に自分で押す。押していない物を司さんに見せない★
  押して「分かりにくい」と思ったら ★見せる前に直す★。
  司さんの1回の確認が一番高い資源。空振りさせない。

### なぜ両方（②と③）要るか
計算libが緑でも「ボタンが押せる／画面が動く／出力が出る／配線されている」は**保証しない**
（過去の「lib は有るのに未配線」バグの型）。

★実際に起きた事故（2026-08-07）★
アマかせのログイン画面が Exally の緑のまま本番に配信されていた。
配信のバイトも確かめ、テストも緑だったが、★誰も本物の画面を開いていなかった★。
司さんが実機で見つけた。**「バイトを測った」は「見た」ではない。**

---

# CLAUDE.md（Rakually）

## ★★このリポジトリは何か★★

**Rakually（ラクアリー）** のテスト線。**Exally ではない**（司さん 2026-08-17
「Rakually は別アプリなんはいつ理解するわけ？」）。

| 何 | repo | URL | 倉庫(DB) |
|---|---|---|---|
| ★テスト線（ここ） | `exally-zeroact/rakually-test` | https://rakually-test.vercel.app/ | **DB-test** |
| ★本番 | `exally-zeroact/rakually` | https://rakually.vercel.app/ | **本番** |

中に入っているアプリ（**1つの器・ログインは1回**）

- `/` … 入口（会社・人・取引先の**共有データ**＋日次台帳＋集計）
- `/kyuyo/` … 給与（明細の作成・配布／管理／従業員のWeb明細）
- `/seikyu/` … 請求書（請求書・見積書・入金）

**★配信は Vercel に揃える。`github.io` は使わない★**（決まり）
**★repo名やホスト名の「test」は環境の証拠にならない★** 証拠は配信された
`js/supa-config.js` の `url` の文字だけ（`tests/pages-hosting.test.mjs` D2 が静的に見張る）。

## ★持って来ていない物（Exally の物・要らん機能）★

`book.html`（表／ブック）・`chat.html`・`hub.html`・`exally-formula.js`・
`hyperformula.full.min.js`・`lib/grid-*.js`・`lib/xlsx-io.js`・`lib/excel-version.js`・
`tests/xlsx-harness/`・`tools/`

**戻す条件** = Rakually に表（ブック）を置くと決めた日。部品・テスト・CIの登録を**まとめて**戻す
（外した記録は `tests/run.js` と `.github/workflows/ci.yml` の頭に理由つきで書いてある）。

**残した物**: `lib/zip-surgeon.js` / `lib/xlsx-edit.js`（**`seikyu/lib/seikyu-book.js` が
require している**＝③自社Excelの土台）と、その唯一の試験 `tests/book-open.test.mjs`
（`js/book-open.js` / `lib/xlsb-edit.js` も試験のために据え置き・**1バイトも変えない**）。

## まだ Exally の名前が残っている所（★客は読まない★ので据え置き）

- ファイル名 `css/rakually-ui.css` / `js/rakually-login.js`
- 中の名前 `RakuallyLogin` / `RakuallyEnvBadge` / `RAKUALLY_EMP_KEYS`

**替えるのは10月のURL切替と同じ塊。** 客が読む字（タブの題・見出し・紙・ログイン画面・
ホーム画面の名前）に他アプリの名前が出ていないかは `tests/own-name.test.mjs` が毎回数える。
`Kyually`（給与の旧製品名）は**期限つきの据え置き**として毎回 件数を出す（2026-10-31まで）。

-----

## 実行方針

- **本番（`rakually`）への push は 司さんの見た目OKの後**。テスト線は先に出してよい。
- **段階導入禁止**＝完全設計 → **1コミット**。
- デプロイ：GitHub push → Vercel 自動（1〜2分）。**`[skip ci]` は書かない**（Vercelも止まる）。
- **実配信を叩き続けない**（403 で自分のテスト環境が止まる）。確認は間を空けて1回、
  **叩いた回数を報告に書く**。

### 認証方式

- GitHub CLI（gh）OAuth のみ。**PATをURLに埋め込み絶対禁止**。

-----

## 禁止事項

- GitHub ウェブエディタ（鉛筆アイコン）使用禁止（Cloudflare 汚染が入る）
- **CSS変数禁止（直接 hex 値のみ）**
- **濃い緑は使用禁止**（コードブロックにも使わない）。全アプリ **#2E7D54** を使う
  → `tests/no-dark-green.test.mjs` が毎回数える
- **`alert` / `prompt` 禁止**（画面が止まる）。知らせはトーストで出す
  → `tests/no-dead-ui.test.mjs` が配信物83本を数える
- **`vercel.json` / `manifest.json` / `package.json` に説明文やメモを書かない**
  （規格外のキーで**ビルド前にデプロイが失敗し、前のデプロイが配信され続ける**）
  → `tests/config-schema.test.mjs`
- **法定の率・額を説明文に直書きしない** → `tests/no-hardcoded-statutory.test.mjs`
- **倉庫の向き先を `js/supa-config.js` 以外に書かない** → `tests/no-hardcoded-supa.test.mjs`

-----

## push 前チェック（必須）

```
node scripts/stamp-build.mjs        # ★JS/CSSを触ったら毎回（?v= の刷り直し）
node tests/run.js                   # 共通（入口UI・共有データ・台帳・集計・配信ガード ほか）
node kyuyo/tests/run.js             # 給与
node seikyu/tests/seikyu-ui.mjs     # 請求書 実UI（全ボタン）
node tests/ci-coverage.test.mjs     # CIから黙って外れたテストが無いか
```

**★ローカル緑 ≠ CI緑★**。`.github/workflows/ci.yml` の `run:` を1行ずつ全部走らせて確かめる。
**★新しい `<app>/tests/` は ci.yml に登録するまで1本も走らない★**（テストを足すコミットと
登録を離さない）。

-----

## ファイル構造

```
rakually-test/
├── index.html          # 入口（共有データ・台帳・集計）
├── js/  lib/  css/  img/   # 共通（入口・給与・請求書が使う）
├── kyuyo/              # 給与（index / admin / meisai）
│   ├── lib/ js/ ops/ tests/ scripts/
├── seikyu/             # 請求書（index 1枚）
│   ├── lib/ js/ tests/
├── api/claude.js       # Vercel の関数（今どの画面からも呼んでいない）
├── scripts/            # stamp-build / dep-count / check-hosts / check-warehouse-pointers
├── tests/              # 共通の見張り
├── docs/HOSTS.md       # ★入口の台帳（増やしたら足す）
└── supabase/           # DDL（配信物ではない）
```

**運ぶ時は `node scripts/dep-count.mjs <入口のHTML>` で「呼ばれる側」を数える**
（呼ぶ側だけ写して本番を白画面にした前科がある。sha一致・CI緑でも捕まらなかった）。

-----

## セッション開始時に必ず読むファイル

- `C:\Users\zeroa\zeroact-memory\START_HERE.md`（状態の唯一の正）
- `C:\Users\zeroa\zeroact-memory\team\global-rules.md`
- `C:\Users\zeroa\zeroact-memory\team\ask-dont-fill.md`（★聞いてあげる。埋めさせない★の正本）

## CIから外しているテスト（1本だけ・機械で見張っている）
- `kyuyo/tests/rakually-login.test.mjs` … item C（メール確認ON前提のsignup分岐）は payslip-app の
  **テスト線のみ**の機能。本番の `js/rakually-login.js` には意図的に未展開で、統合時に本番のログイン
  挙動を変えないため共通部品は本番版を据え置いた。よって対象機能がこのツリーに無い。
  **戻す条件＝item C を本番へ展開する時（＝SMTPを設定してメール確認をONにする時）。**
  その時に `js/rakually-login.js` を item C 版へ差し替え、このテストをCIへ戻す。
- 除外を増やすと `tests/ci-coverage.test.mjs` が赤になる（理由と戻す条件が無い除外も赤）。
  CIから黙って外れたテストが生まれない仕組み。
